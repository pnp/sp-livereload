export interface ILiveReloaderState {
    available: boolean,
    connected: boolean,
    debugConnected: boolean
}

export interface ILiveReloaderSession {
    connected: boolean,
    debugConnected: boolean
}